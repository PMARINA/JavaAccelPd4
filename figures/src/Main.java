/*
 * Using BlueJ’s video notes and source files on edmodo.com, create a new picture where there are more than one person in different locations, a house, a sun and grass.
 * @author PMARINA
 * @version Feb 8, 2016
 */
public class Main {
	/**
	 * Main method, creates a house and a person who is aimlessly walking back and forth near the house
	 * @params param1 does this
	 * @returns returns stuff
	 * @throws noInputError no input given by the user
	 */
	public static void main(String[] args) {
		Picture myPic = new Picture();
		myPic.draw();
		myPic.sunSet(3);
	}
}